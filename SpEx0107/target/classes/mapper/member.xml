<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mabatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="co.syeon.spex.member.service.MemberDAO">
	<select id="memberList" resultType="co.syeon.spex.member.vo.MemberVO">
		SELECT * FROM MEMBER		
	</select>
	<select id="memberSelect" resultType="co.syeon.spex.member.vo.MemberVO">
		SELECT * FROM memeber WHERE memberid = #{memberid}
	</select>
	<insert id=""></insert>
	<update id=""></update>
	<delete id=""></delete>
</mapper>